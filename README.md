# ReflectionWithSQLServer
Utiliza C# Reflection para generar automáticamente sentencias SQL (INSERT, UPDATE, DELETE, SELECT y CREATE TABLE) basadas en las clases de entidades definidas en el proyecto.
